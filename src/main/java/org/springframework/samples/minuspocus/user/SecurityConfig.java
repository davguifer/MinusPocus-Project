package org.springframework.samples.minuspocus.user;

/* 
@Configuration
public class SecurityConfig {

    // Otros métodos de configuración pueden estar presentes...

    @Bean
    public PasswordEncoder passwordEncoder(String cad) {
        return new BCryptPasswordEncoder();
    }
}
*/